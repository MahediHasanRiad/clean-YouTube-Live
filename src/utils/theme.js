export const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    background: {
      default: "#000000", 
    },
    text: {
      primary: '#ffffff'
    }
  },
});

export const lightTheme = createTheme({
  palette: {
    mode: 'light',
    background: {
      default: '#ffff'
    },
    text: {
      primary: '#0000'
    }
  }
})
